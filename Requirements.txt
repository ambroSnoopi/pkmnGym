############################# Requirements for ML Models (locally) #############################
# This is only required, if the ONLINE_AI Setting is turned-off
# Run one of the following to install the required packages in your python environment:
#
#   python -m pip install -r Requirements.txt
#
# The command should be executed from a cmd line tool at the location of this file.
################################################################################################

numpy
pandas
joblib
azureml
azureml.automl.core
azureml.automl.runtime
inference_schema
#sklearn
scikit-learn


#Examples:
#numpy == 1.21.2
#pandas >= 1.3.4
#pip != 20.2.1
#dateutils ~= 0.6.12  #Compatible release. Same as >= 0.6.12  or  == 0.6.*